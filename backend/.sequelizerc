const { resolve } = require('path');

module.exports = {
  config: resolve('src', 'config', 'migrationConfig.js'),
  'migrations-path': resolve('src', 'migrations'),
  'models-path': resolve('src', 'models'),
};
